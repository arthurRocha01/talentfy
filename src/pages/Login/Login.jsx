import { Input } from '../../shared/components/Input.jsx';

export const Login = () => {
    return (
        <div className="container">
            <Input placeholder="Input..." />
        </div>
    );
}