import { Button } from "../../shared/components/Button.jsx";
import { Input } from "../../shared/components/Inputjsx";

export const Register = () => {
    return (
        <div className="container">
            <Input placeholder="Input..." />
            <Button text="Registrar" />
        </div>
    );
}